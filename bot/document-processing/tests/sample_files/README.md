# Sample Files

This directory is reserved for small document fixtures when manual fixture files are useful. The current automated tests generate their PDF, DOCX, and TXT inputs at runtime so the repository remains lightweight and reproducible.
